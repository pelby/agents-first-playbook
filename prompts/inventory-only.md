# Inventory-Only Prompt

I want to understand my current agent configuration before changing anything.

Do not edit, move, delete, or create files.

Please inspect:

- `~/.agents`
- `~/.claude`
- `~/.codex`
- `~/.gemini`
- `~/Workspaces`

Report:

- which instruction files exist
- which are real files vs symlinks
- where symlinks point
- which file appears to be canonical today
- broken symlinks
- conflicts where both adapter and canonical files are real
- tool runtime folders that should not be moved
- likely private files that should never be published

Use concrete paths. Do not print private file contents unless I explicitly ask.
